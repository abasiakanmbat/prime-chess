# Optional: Backend API URL override
# By default, the app uses the current app URL as the API URL
# Only set this if you need to use a different API server
# For example, if your API is on a separate domain:
# VITE_API_URL=https://api.yourdomain.com
